Configuration

Environment Variables

Required or optional environment variables include DISCORD_TOKEN, LLM_API_KEY, and GOOGLE_CALENDAR_CREDENTIALS (optional).

config.yaml

Configuration specifies enabled modules, confidence thresholds, schedules, reminder defaults, repo paths, and index paths. Configuration drives composition and sets the confidence gate for automatic actions.
