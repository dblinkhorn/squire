Next steps:
1) Add a tiny CLI script to create an admin item using the canonical store + schema.
2) Add a minimal rebuild-index CLI command (reads paths from config).
3) Implement a simple find query against SQLite (keyword + optional tag).
4) Wire in optional tag extraction from inline #hashtags.
